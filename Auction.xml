<contract name="Auction 2 example"
      invariant="TRUE"
>
    <variable name="hBidder" type="INT" />
    <variable name="hBid" type="REAL" />
    <variable name="ended" type="BOOLEAN" />
    <variable name="won" type="BOOLEAN" />
    <constructor name="Auction"
            pre="TRUE"
            post="hBidder' = -1 AND hBid' = 0 AND ended' &lt;=> FALSE AND won' &lt;=> FALSE">
    </constructor>
    <action name="bid"
            pre="bidder >= 0 AND bid > 0 AND bid > hBid AND bidder /= hBidder AND ended &lt;=> FALSE"
            post="hBid' = bid AND hBidder' = bidder">
      <parameter name="bidder" type="INT" />
      <parameter name="bid" type="REAL" />
    </action>
    <action name="win"
            pre="bidder = hBidder AND ended AND won &lt;=> FALSE"
            post="won'">
      <parameter name="bidder" type="INT" />
    </action>
    <action name="finish"
            pre="ended &lt;=> FALSE"
            post="ended'">
    </action>
</contract>